# Copy to .env and adjust for your setup.

# Workspace dirs to split by (colon-separated). Used by claude_cache_by_project.py.
CACHE_GROUPS=~/dev/projects:~/dev/work

# Override the default Claude Code sessions directory. Used by both scripts.
# SESSIONS_DIR=~/.claude/projects
